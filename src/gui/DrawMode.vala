namespace Planly
{
    /** Modo de dibujo activo en el Scene. */
    public enum DrawMode {
        /** Líneas libres en cualquier ángulo. */
        NORMAL,
        /** Líneas restringidas a 8 direcciones (múltiplos de 45°). */
        FLATTEN
    }
}
